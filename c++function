#include <iostream>
using namespace std;

void print(string text) {
  cout << text << endl;
}

void printNTimes(int n, string text) {
  for (int i = 0; i < n; i++) {
      print(text);
  }
}

int main() {
  printNTimes(5, "Surender")
  printNTtimes(7, "Is Great");
}
